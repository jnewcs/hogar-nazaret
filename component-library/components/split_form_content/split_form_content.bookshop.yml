# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
  label: Split Form Content
  description: Generic text component with option to show form to the right of content
  icon: vertical_split
  image: /uploads/components/split_form_content.preview.png
  tags:
    - Text

# Defines the structure of this component, as well as the default values
blueprint:
  content_html: ""
  form: bookshop:form

# Overrides any fields in the blueprint when viewing this component in the component browser
preview:
  content_html: <p>Praesent commodo cursus magna, vel scelerisque nisl consectetur et.</p>

# Any extra CloudCannon inputs configuration to apply to the blueprint
_inputs:
  content_html:
    type: html
    label: Content Text
    allow_empty: false
