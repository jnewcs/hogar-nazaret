collections_config:
  noticias:
    sort:
      key: date
    add_options:
      - name: Add Noticia for Spanish Site
        editor: content
    schemas:
      default:
        path: schemas/noticia.md
        _enabled_editors:
          - visual
          - content
          - data
        hide_extra_inputs: true
        _inputs:
          $.date:
            label: Publish Date
            allow_empty: false
          $.banner_image:
            label: Preview Image
            comment: This image is used in list view and certain layouts like the `side_by_side` layout
            type: image
            allow_empty: true
          $.banner_image_alt:
            label: Preview image description
            comment: Adding a description benefits visually impaired users who use screen readers when browsing. Should be written in Spanish
            allow_empty: true
          $.banner_image_style:
            label: Preview image style
            type: select
            options:
              values:
                - cover
                - contain
              allow_create: false
              allow_empty: false
          $.content_blocks:

  posts:
    sort:
      key: date
    add_options:
      - name: Add News Article for English Site
        editor: content
    schemas:
      default:
        path: schemas/post.md
        _enabled_editors:
          - visual
          - content
          - data
        hide_extra_inputs: true
        _inputs:
          $.date:
            label: Publish Date
            allow_empty: false
          $.banner_image:
            label: Preview Image
            comment: This image is used in list view and certain layouts like the `side_by_side` layout
            type: image
            allow_empty: true
          $.banner_image_alt:
            label: Preview image description
            comment: Adding a description benefits visually impaired users who use screen readers when browsing. Should be written in English
            allow_empty: true
          $.banner_image_style:
            label: Preview image style
            type: select
            options:
              values:
                - cover
                - contain
              allow_create: false
              allow_empty: false
          $.content_blocks:

  carousel_highlights_es:
    name: Carousel Highlights
    sort:
      key: creation_date
    add_options:
      - name: Add Carousel Highlight for Spanish Site
        editor: visual
    disable_add_folder: true
    path: _carousel_highlights/es/
    schemas:
      default:
        path: schemas/carousel_highlights_es.md
        _enabled_editors:
          - visual
          - content
          - data
        hide_extra_inputs: true
        _inputs:
          $.creation_date:
            label: Publish Date
            allow_empty: false

  carousel_highlights_en:
    name: Carousel Highlights
    sort:
      key: creation_date
    add_options:
      - name: Add Carousel Highlight for English Site
        editor: visual
    disable_add_folder: true
    path: _carousel_highlights/en/
    schemas:
      default:
        path: schemas/carousel_highlights_en.md
        _enabled_editors:
          - visual
          - content
          - data
        hide_extra_inputs: true
        _inputs:
          $.creation_date:
            label: Publish Date
            allow_empty: false

collection_groups:
  - heading: "Spanish Website"
    collections:
      - noticias
      - carousel_highlights_es
  - heading: "English Website"
    collections:
      - posts
      - carousel_highlights_en

_inputs:
  $.lang:
    hidden: true
  $.redirect_from:
    hidden: true
  $.layout:
    hidden: true
  $.lang_ref:
    label: Identifier to link page to English/Spanish equivalent
    allow_empty: true
